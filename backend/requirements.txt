# SafePath API dependencies.
fastapi==0.115.6
uvicorn[standard]==0.34.0
httpx==0.28.1
python-dotenv==1.0.1
langfuse>=3,<4

# Authentication dependencies.
python-jose[cryptography]

# Database dependencies
sqlalchemy>=2.0,<2.1
psycopg[binary]>=3.1

# safepath-mcp service dependencies (same image, different command — see docker-compose.dev.yml)
# Pinned below 2.0: mcp 2.0.0 renamed FastMCP -> MCPServer and moved it to
# mcp.server.mcpserver (dropping mcp.server.fastmcp + the host/port/transport_security
# constructor args our mcp_server.py relies on). Revisit this pin if/when we migrate.
mcp[cli]>=1.27,<2.0
shapely
openpyxl
pyproj
cachetools
# Was already an installed transitive dep of shapely/pyproj; pinned explicitly
# now that accident_lookup.py/lighting_lookup.py use it directly to vectorize
# the per-route-sample-point distance lookups (was a brute-force Python loop
# over ~61.7k accidents + ~101.8k lamps per point — see the get_safe_routes
# latency investigation for the before/after numbers).
numpy
